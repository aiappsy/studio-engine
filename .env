# Which provider to use by default if the client does not specify
AI_PROVIDER=openrouter

# Optional fallback key for internal testing (WebStudio will send its own keys)
AI_API_KEY=

# Default model to use
AI_MODEL=gpt-4o-mini

# Port (Fastify listens on 3001 by default)
PORT=3001

# Workspace path (Coolify will mount a volume here)
WORKSPACE_ROOT=/data/workspaces

# Logging level
LOG_LEVEL=info
